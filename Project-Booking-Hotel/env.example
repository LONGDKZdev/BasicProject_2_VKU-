# ============================================
# SUPABASE CONFIGURATION (Primary Database)
# ============================================
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# ============================================
# C# BACKEND API (Authentication & Fallback)
# ============================================
# C# API URL - Dùng cho authentication (OAuth, Password Reset)
# Nếu không set hoặc C# API không khả dụng, sẽ tự động fallback sang Supabase
VITE_API_URL=http://localhost:5000

# ============================================
# EMAIL SERVICE (Optional)
# ============================================
# EmailJS configuration (chỉ dùng nếu không dùng C# API email service)
VITE_EMAILJS_SERVICE_ID=your-service-id
VITE_EMAILJS_TEMPLATE_ID=your-template-id
VITE_EMAILJS_PUBLIC_KEY=your-public-key

# ============================================
# DUAL API STRATEGY
# ============================================
# Hệ thống sẽ tự động:
# 1. Ưu tiên gọi C# API trước (timeout 5s)
# 2. Nếu C# API fail/timeout → tự động fallback sang Supabase
# 3. User không bị gián đoạn, hệ thống luôn hoạt động